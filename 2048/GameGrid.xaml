<UserControl
    x:Class="_2048.GameGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Background="#776e65">

    <Viewbox>
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Name="ContentGrid">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Grid.Row="1" BorderThickness="10,10,10,10" BorderBrush="#776e65" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CornerRadius="5" Background="#776e65">
                <Canvas Background="#776e65" Name="GameCanvas" Width="700" Height="700">
                </Canvas>
            </Border>
        </Grid>
    </Viewbox>
</UserControl>
